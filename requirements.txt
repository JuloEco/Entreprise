Flask==3.0.3
Werkzeug==3.0.3

# Postgres (Neon ou tout autre Postgres managé) — backend recommandé en
# production. Fournit psycopg2 avec les binaires précompilés, pas besoin de
# toolchain de compilation sur Vercel.
psycopg2-binary

# Conservé pour compatibilité avec un déploiement existant sur Turso.
# Optionnel dès lors que DATABASE_URL (Postgres) est configuré.
# Pas de version épinglée volontairement : vérifiez la dernière version
# stable sur https://pypi.org/project/libsql/ au moment du déploiement.
libsql
